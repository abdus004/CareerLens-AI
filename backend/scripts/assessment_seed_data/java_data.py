# Java category - 40 Easy + 40 Medium + 40 Hard = 120 real, hand-written
# MCQs. Each tuple is:
# (topic, question, option_a, option_b, option_c, option_d, correct_letter, explanation)

JAVA_EASY = [
    ("Java Basics", "Which method is the entry point of a standalone Java application?", "start()", "main()", "run()", "init()", "B", "The Java Virtual Machine looks for a public static void main(String[] args) method as the program's entry point."),
    ("Java Basics", "Which of these is used to compile a Java source file?", "java", "javac", "javap", "jar", "B", "javac is the Java compiler, which turns .java source files into .class bytecode files."),
    ("Java Basics", "What is the correct file extension for a compiled Java bytecode file?", ".java", ".class", ".jar", ".exe", "B", "Compiling a .java file with javac produces a .class file containing JVM bytecode."),
    ("Java Basics", "What does JVM stand for?", "Java Virtual Machine", "Java Verified Method", "Java Variable Manager", "Java Visual Model", "A", "The JVM (Java Virtual Machine) is what actually executes compiled Java bytecode, providing platform independence."),
    ("Data Types", "Which of these is a primitive data type in Java?", "String", "int", "ArrayList", "Object", "B", "int is one of Java's eight primitive types; String, ArrayList, and Object are all reference (object) types."),
    ("Data Types", "What is the default value of a boolean instance variable in Java if not explicitly initialized?", "true", "false", "null", "0", "B", "Uninitialized boolean fields default to false in Java."),
    ("Data Types", "Which keyword declares a constant whose value cannot be changed after assignment?", "const", "final", "static", "constant", "B", "The final keyword, when applied to a variable, prevents its value from being reassigned after initialization."),
    ("Loops", "Which loop is guaranteed to execute its body at least once in Java?", "for loop", "while loop", "do-while loop", "for-each loop", "C", "A do-while loop checks its condition after running the body, so the body always executes at least once."),
    ("Loops", "What does the 'continue' statement do inside a Java loop?", "Immediately exits the loop entirely", "Skips the rest of the current iteration and proceeds to the next one", "Pauses the loop indefinitely", "Restarts the entire program", "B", "continue skips any remaining code in the current iteration and moves on to the loop's next iteration check."),
    ("Loops", "How many times does 'for (int i = 0; i < 5; i++)' execute its body?", "4", "5", "6", "Infinite", "B", "i starts at 0 and the loop runs while i < 5, so it executes for i = 0,1,2,3,4 - five times total."),
    ("Methods", "What is required to define a method in Java?", "A return type, a name, and a parameter list (possibly empty)", "Only a name", "Only a return type", "A class definition is not required for methods", "A", "A Java method requires a return type (or void), a method name, and a parameter list, even if that list is empty."),
    ("Methods", "What does the 'void' return type indicate for a method?", "The method returns an integer", "The method does not return any value", "The method returns a null object", "The method cannot be called", "B", "void explicitly indicates the method performs an action but returns no value to the caller."),
    ("Methods", "What is method overloading in Java?", "Defining multiple methods with the same name but different parameter lists in the same class", "Calling a method too many times", "A method that calls itself", "Deleting a method from a class", "A", "Overloading lets a class define several methods sharing a name but differing in parameter type/count, resolved at compile time based on the arguments passed."),
    ("Classes and Objects", "What is a class in Java?", "An instance of an object", "A blueprint or template used to create objects", "A built-in Java keyword only", "A type of loop", "B", "A class defines the structure (fields) and behavior (methods) that its objects (instances) will have."),
    ("Classes and Objects", "What is an object in Java?", "A specific instance of a class, with its own state", "A synonym for a class itself", "A primitive data type", "A type of exception", "A", "An object is a concrete instance created from a class, with its own values stored in the class's defined fields."),
    ("Classes and Objects", "Which keyword is used to create a new object in Java?", "create", "make", "new", "object", "C", "The 'new' keyword allocates memory for a new object and calls its constructor, e.g. Car myCar = new Car();."),
    ("Classes and Objects", "What is a constructor in Java?", "A method that deletes an object", "A special method automatically called when an object is created, typically used to initialize its fields", "A keyword used only for loops", "A type of interface", "B", "A constructor shares the class's name and has no return type; it runs automatically when 'new' creates an instance, typically setting up initial state."),
    ("Inheritance", "Which keyword allows a Java class to inherit from another class?", "inherits", "extends", "implements", "super", "B", "The 'extends' keyword is used for class inheritance, e.g. class Dog extends Animal {}."),
    ("Inheritance", "In Java, how many classes can a single class directly extend?", "Unlimited", "Exactly one (single inheritance for classes)", "Exactly two", "Zero, classes can never extend another class", "B", "Java supports single inheritance for classes - a class can extend only one direct superclass (though that superclass can have its own superclass)."),
    ("Inheritance", "What does the 'super' keyword refer to inside a subclass?", "The subclass itself", "The immediate parent (super) class", "A static utility class", "An external library", "B", "'super' is used to access the parent class's constructor, methods, or fields from within a subclass."),
    ("Inheritance", "What is a 'subclass' in Java terminology?", "A class with no methods", "A class that inherits from another class (its superclass)", "A private inner class only", "A class that cannot be instantiated", "B", "A subclass extends a superclass, inheriting its accessible fields and methods while potentially adding or overriding behavior."),
    ("Polymorphism", "What does polymorphism in Java generally allow?", "A single method call to behave differently depending on the actual object type at runtime", "Every class to have exactly one method", "Variables to change their declared type at runtime", "Classes to inherit from multiple classes simultaneously", "A", "Polymorphism lets code call the same method name on different object types, with each object's specific implementation running - a key OOP concept."),
    ("Polymorphism", "What is method overriding in Java?", "Defining two methods with the same name and same parameters in unrelated classes", "A subclass providing its own specific implementation of a method already defined in its superclass", "Deleting an inherited method", "Calling a method multiple times", "B", "Overriding happens when a subclass redefines an inherited method with the same signature, replacing the parent's behavior for that subclass's instances."),
    ("Abstraction", "What is an abstract class in Java?", "A class that can never be inherited from", "A class that cannot be instantiated directly and may contain both abstract and concrete methods", "A class with no fields", "A synonym for an interface", "B", "An abstract class (declared with 'abstract') cannot be instantiated on its own and is meant to be subclassed, potentially containing unimplemented (abstract) methods."),
    ("Abstraction", "What must a concrete (non-abstract) subclass do with its abstract superclass's abstract methods?", "It can ignore them entirely", "It must provide an implementation for all inherited abstract methods, or remain abstract itself", "It must delete them", "It must convert them into fields", "B", "Any concrete subclass is required to implement every abstract method it inherits; otherwise it must also be declared abstract."),
    ("Interfaces", "What is an interface in Java primarily used to define?", "A concrete implementation of a class", "A contract of method signatures that implementing classes must fulfill", "A private helper class", "A loop structure", "B", "An interface declares method signatures (a contract) without providing implementation, which implementing classes must then define."),
    ("Interfaces", "Which keyword does a class use to indicate it implements an interface?", "extends", "implements", "inherits", "uses", "B", "Classes use 'implements' to indicate they fulfill an interface's contract, e.g. class Car implements Drivable {}."),
    ("Interfaces", "Can a Java class implement more than one interface?", "No, only one interface is allowed", "Yes, a class can implement multiple interfaces", "Only abstract classes can implement interfaces", "Only if the interfaces are empty", "B", "Unlike single class inheritance, Java allows a class to implement multiple interfaces simultaneously."),
    ("Exception Handling", "Which block is used to catch and handle exceptions in Java?", "try/handle", "try/catch", "try/rescue", "try/fix", "B", "Java uses try/catch blocks, where code that might throw is placed in try, and the exception is handled in catch."),
    ("Exception Handling", "Which block always executes, whether or not an exception was thrown, in Java?", "always", "finally", "ensure", "final", "B", "The finally block runs regardless of whether the try block completed normally or an exception was thrown and caught."),
    ("Exception Handling", "Which keyword is used to manually throw an exception in Java?", "throw", "raise", "error", "except", "A", "The 'throw' keyword is used to explicitly throw an exception object, e.g. throw new IllegalArgumentException();."),
    ("Collections", "Which interface represents an ordered collection that allows duplicate elements in Java?", "Set", "List", "Map", "Queue only", "B", "The List interface (implemented by ArrayList, LinkedList, etc.) maintains insertion order and permits duplicate elements."),
    ("Collections", "Which collection type stores key-value pairs in Java?", "List", "Set", "Map", "Array", "C", "The Map interface (implemented by HashMap, TreeMap, etc.) associates unique keys with values."),
    ("Collections", "Which built-in Java collection class does NOT allow duplicate elements?", "ArrayList", "LinkedList", "HashSet", "Vector", "C", "Set implementations like HashSet enforce uniqueness, automatically ignoring attempts to add a duplicate element."),
    ("Collections", "What is the main purpose of the Java Collections Framework?", "To replace all primitive data types", "To provide a unified architecture for storing and manipulating groups of objects (lists, sets, maps, etc.)", "To handle only file input/output", "To compile Java programs", "B", "The Collections Framework provides standard interfaces and implementations (List, Set, Map, etc.) for working with groups of objects consistently."),
    ("Strings", "Are Java Strings mutable or immutable?", "Mutable - they can be changed in place", "Immutable - a new String object is created for modifications", "It depends on the JVM version", "Only mutable inside loops", "B", "Java String objects are immutable; operations like concatenation create and return a new String rather than modifying the original."),
    ("Strings", "Which method compares the content of two Java String objects for equality?", "==", ".equals()", ".compare()", ".same()", "B", ".equals() compares the actual character content of two strings, while == compares object references."),
    ("Collections", "Which of these is a valid way to create an empty ArrayList of Strings in Java?", "ArrayList<String> list = new ArrayList<>();", "ArrayList list = ArrayList.new();", "String[] list = ArrayList();", "list = new String[ArrayList];", "A", "The 'new ArrayList<>()' constructor call creates a new, empty ArrayList instance, here typed to hold String elements."),
    ("Abstraction", "Which keyword marks a Java class or method as abstract?", "abstract", "virtual", "interface", "hidden", "A", "The 'abstract' keyword marks a class as non-instantiable or a method as having no body, requiring subclasses to implement it."),
    ("Interfaces", "Can a Java interface contain method signatures with no implementation (body)?", "No, all interface methods must have a body", "Yes, that is the traditional purpose of an interface - declaring signatures implementing classes must define", "Only if the interface is marked final", "Only in unit test code", "B", "Traditionally, an interface's methods are abstract by default, declaring only the method signature that implementing classes are required to provide a body for."),
]

JAVA_MEDIUM = [
    ("Java Basics", "What does 'public static void main(String[] args)' signify about the main method?", "It can only be called from within the same class", "It is accessible from anywhere, belongs to the class itself (not an instance), and returns nothing", "It must return an integer exit code directly", "It is a constructor", "B", "public makes it accessible to the JVM launcher, static means it belongs to the class rather than an instance (so the JVM can call it without creating an object first), and void means it returns nothing."),
    ("Java Basics", "What is the purpose of the classpath in a Java application?", "It defines which class runs first alphabetically", "It tells the JVM/compiler where to look for compiled classes and libraries needed by the program", "It only matters for GUI applications", "It replaces the need for import statements", "B", "The classpath specifies the directories/JAR files the JVM and compiler search when resolving class references at compile and run time."),
    ("Data Types", "What is autoboxing in Java?", "Manually converting a primitive to its wrapper class using explicit casting only", "The automatic conversion Java performs between a primitive type and its corresponding wrapper class, e.g. int to Integer", "A way to declare arrays automatically", "A deprecated feature no longer supported", "B", "Autoboxing lets Java implicitly convert primitives to their wrapper objects (and unboxing does the reverse) where an object type is expected, such as adding an int to a List<Integer>."),
    ("Data Types", "What happens when you divide two integers in Java, such as 7 / 2?", "It returns 3.5", "It performs integer division, truncating toward zero, returning 3", "It throws an exception", "It automatically returns a double", "B", "Integer division in Java truncates any fractional part, so 7 / 2 evaluates to 3, not 3.5."),
    ("Loops", "What is the difference between a Java 'for' loop and an 'enhanced for' (for-each) loop?", "There is no functional difference at all", "The enhanced for loop iterates directly over elements of an array/collection without manual indexing, but cannot easily access the index or modify the underlying structure by index", "The enhanced for loop can only be used with primitive arrays", "A regular for loop cannot iterate over arrays", "B", "The for-each loop simplifies iterating over collections/arrays by hiding the index management, at the cost of not directly exposing the current index or supporting index-based modification."),
    ("Methods", "What is the difference between passing a primitive versus an object reference as a method argument in Java?", "Both are always passed by reference, allowing the method to change the caller's variable", "Primitives are passed by value (a copy), while object references are also passed by value, but that value is a reference, so mutating the referenced object's fields IS visible to the caller", "Objects are always copied entirely before being passed", "Primitives cannot be passed as method arguments", "B", "Java is strictly pass-by-value: for primitives, the value itself is copied; for objects, the reference value is copied, so reassigning the parameter inside the method doesn't affect the caller, but modifying the object's fields through that reference does."),
    ("Methods", "What is method overloading resolution based on in Java?", "The method's return type alone", "The number, types, and order of arguments in the method call, resolved at compile time", "The order methods are declared in the source file", "Random selection at runtime", "B", "The compiler chooses which overloaded method to call by matching the call's argument types/count/order against the available overloaded signatures - this happens statically, at compile time."),
    ("Classes and Objects", "What is the purpose of the 'this' keyword inside an instance method?", "It refers to the class definition itself", "It refers to the current object instance the method was called on, often used to disambiguate fields from parameters with the same name", "It is only used in static methods", "It refers to the parent class", "B", "'this' explicitly refers to the calling instance, commonly used as 'this.field = field;' inside a constructor when a parameter shares a name with an instance field."),
    ("Classes and Objects", "What does it mean for a field to be declared 'static' in a Java class?", "Each object instance gets its own independent copy", "The field belongs to the class itself, shared across all instances, rather than being per-object", "The field cannot ever change its value", "The field is automatically private", "B", "A static field is stored once per class (not once per instance), so all objects of that class share and see the same value."),
    ("Inheritance", "What happens if a subclass does not explicitly call a specific superclass constructor?", "The subclass fails to compile", "Java automatically inserts an implicit call to the superclass's no-argument constructor before the subclass constructor's body runs", "The superclass is simply skipped entirely", "The object is created without any superclass initialization", "B", "Unless you explicitly call super(...) with arguments, Java implicitly inserts a call to the superclass's no-arg constructor as the first statement of the subclass constructor."),
    ("Inheritance", "Why does Java not support multiple inheritance of classes, but does allow implementing multiple interfaces?", "Interfaces are functionally identical to classes with no real distinction", "Multiple class inheritance can create ambiguity (e.g. the diamond problem) for conflicting method implementations, while interfaces (traditionally) only declared method signatures with no state, avoiding that specific ambiguity", "Java simply forgot to implement multiple class inheritance", "Interfaces cannot have any methods at all", "B", "By restricting classes to single inheritance while allowing multiple interface implementation, Java sidesteps the classic diamond-inheritance ambiguity around which parent's conflicting implementation should apply."),
    ("Polymorphism", "What is 'runtime polymorphism' (dynamic method dispatch) in Java primarily achieved through?", "Method overloading, resolved at compile time", "Method overriding, where the JVM determines at runtime which overridden version to call based on the object's actual runtime type", "Static methods only", "Only through interfaces, never through class inheritance", "B", "When a superclass reference points to a subclass object and calls an overridden method, the JVM looks up the actual object's runtime type to decide which version executes - this is dynamic dispatch."),
    ("Polymorphism", "Given 'Animal a = new Dog();' where Dog overrides a method 'sound()' from Animal, which version runs when a.sound() is called?", "Animal's version, since 'a' is declared as type Animal", "Dog's overridden version, because Java resolves overridden instance methods based on the object's actual runtime type", "Neither; this causes a compile error", "It depends on which class was compiled first", "B", "Even though the reference variable's declared type is Animal, Java's dynamic dispatch calls the overridden method based on the actual object created (Dog) at runtime."),
    ("Abstraction", "What is the key practical difference between an abstract class and an interface (in modern Java, which allows default methods)?", "They are now functionally identical in every case", "An abstract class can maintain instance state via fields and supports single inheritance, while a class can implement many interfaces but interfaces traditionally focus on behavior contracts", "Interfaces can have constructors but abstract classes cannot", "Abstract classes cannot have any methods at all", "B", "Even with default methods, interfaces still cannot hold traditional mutable instance state the way an abstract class's fields can, and a class remains limited to extending only one abstract class while implementing multiple interfaces."),
    ("Abstraction", "Why might a designer choose an abstract class over an interface when several related classes share common field-based behavior?", "Interfaces execute faster at runtime", "An abstract class lets you define shared fields and partially implemented (concrete) methods that subclasses inherit directly, reducing duplicated logic", "Abstract classes are required for any class hierarchy", "There is no meaningful difference in this scenario", "B", "When subclasses genuinely share state and reusable logic (not just a method contract), an abstract class lets that common implementation live in one place and be inherited, which a pure interface contract doesn't provide as naturally."),
    ("Interfaces", "What is a 'default method' in a Java interface (introduced in Java 8)?", "A method that is always abstract and must be overridden", "A method with a concrete body defined directly in the interface, which implementing classes can use as-is or override", "A method that only exists in abstract classes", "A method that can never be called", "B", "Default methods let interfaces provide a ready-made implementation, allowing new methods to be added to interfaces without breaking every existing implementing class."),
    ("Interfaces", "What is the effect of two interfaces both providing a default method with the exact same signature, when a single class implements both?", "Java automatically picks the first interface listed", "The implementing class must explicitly override that method to resolve the conflict, or a compile error occurs", "Only the interface with fewer methods 'wins'", "The class silently inherits both implementations simultaneously", "B", "When default method signatures collide across multiple implemented interfaces, Java requires the implementing class to explicitly resolve the ambiguity by overriding the method itself."),
    ("Exception Handling", "What is the difference between a checked and an unchecked exception in Java?", "There is no real difference; the terms are interchangeable", "Checked exceptions must be either caught or declared with 'throws' at compile time, while unchecked exceptions (RuntimeException and its subclasses) are not enforced by the compiler", "Unchecked exceptions can never be caught", "Checked exceptions only occur during compilation, never at runtime", "B", "The Java compiler enforces that checked exceptions (like IOException) are handled or declared, while unchecked exceptions (like NullPointerException) can occur without any such compile-time requirement."),
    ("Exception Handling", "What does the 'throws' clause in a method signature indicate?", "The method will definitely throw an exception every time it's called", "The method may throw the specified checked exception(s), and callers must handle or further declare them", "It automatically catches all exceptions inside the method", "It is required for every Java method regardless of behavior", "B", "'throws' in a method signature is a declaration that the method might propagate that checked exception, obligating callers to handle it or declare it themselves."),
    ("Collections", "What is the main structural difference between ArrayList and LinkedList in Java?", "They are functionally identical with no performance differences", "ArrayList is backed by a dynamic array (fast random access, slower inserts/removals in the middle), while LinkedList is backed by a doubly linked list (slower random access, faster inserts/removals at known positions)", "LinkedList cannot store objects, only primitives", "ArrayList cannot be resized after creation", "B", "ArrayList's array-backed structure gives O(1) indexed access but O(n) mid-list insertion/removal, while LinkedList's node-based structure gives O(1) insertion/removal at a known node but O(n) indexed access."),
    ("Collections", "Why is HashMap generally not suitable when you need predictable iteration order matching insertion order?", "HashMap cannot store more than a few elements", "HashMap's iteration order is based on hash bucket placement, not insertion order; LinkedHashMap should be used instead for that guarantee", "HashMap always iterates in reverse insertion order", "HashMap does not support iteration at all", "B", "HashMap makes no ordering guarantee since it organizes entries by hash code, whereas LinkedHashMap explicitly maintains a linked list to preserve insertion (or access) order during iteration."),
    ("Strings", "Why is StringBuilder generally preferred over repeated String concatenation ('+') inside a loop?", "StringBuilder cannot be used outside of loops", "Because Strings are immutable, repeated concatenation creates many intermediate String objects, while StringBuilder mutates an internal buffer in place, which is far more efficient for many appends", "String concatenation is not valid Java syntax", "StringBuilder objects are immutable, just like String", "B", "Since each '+' on immutable Strings can produce a new String object, doing this repeatedly in a loop wastes memory and time; StringBuilder avoids this by appending to a single resizable internal buffer."),
    ("Strings", "What is the practical difference between String and StringBuilder regarding thread safety, compared to StringBuffer?", "String, StringBuilder, and StringBuffer are all thread-safe by default", "String is immutable and therefore inherently safe to share across threads; StringBuilder is not synchronized (faster, single-threaded use), while StringBuffer is synchronized (thread-safe, with some overhead)", "StringBuilder is always slower than StringBuffer", "None of these classes can be used in multithreaded code", "B", "String's immutability makes it safe to share freely; StringBuilder trades away synchronization for speed in single-threaded contexts, while StringBuffer keeps synchronization for safe concurrent access at some performance cost."),
    ("Java Basics", "What is the purpose of the 'package' declaration at the top of a Java source file?", "It has no functional effect, purely cosmetic", "It groups related classes/interfaces together under a namespace, helping avoid naming conflicts and organizing code", "It replaces the need for a class declaration", "It is only used for GUI applications", "B", "Packages organize related types under a hierarchical namespace, which also helps avoid class name collisions between unrelated libraries."),
    ("Data Types", "Why might using 'float' instead of 'double' for a value requiring high precision be risky in Java?", "float always throws a compile error when used", "float has less precision (about 7 decimal digits) than double (about 15-16 decimal digits), which can introduce larger rounding errors in calculations", "float cannot store decimal values at all", "double is deprecated in modern Java", "B", "float uses 32 bits and offers roughly single-precision accuracy, while double uses 64 bits for roughly double the precision - using float where high precision matters can accumulate noticeable rounding error."),
    ("Loops", "What is the effect of a labeled break statement, such as 'break outerLoop;', inside a nested loop in Java?", "It behaves identically to an unlabeled break, exiting only the innermost loop", "It exits the specifically labeled outer loop directly, skipping the need for a boolean flag to break out of multiple nested loops", "It is invalid syntax in Java", "It restarts the outer loop from the beginning", "B", "A labeled break lets you jump out of a specifically named enclosing loop directly, which is useful for exiting multiple nested loops at once without extra flag variables."),
    ("Methods", "What does Java's varargs syntax, e.g. 'void print(String... items)', allow a caller to do?", "Call the method with a fixed, unchangeable number of arguments only", "Call the method with zero, one, or many String arguments, which are automatically collected into an array inside the method", "Pass only a single array explicitly and nothing else", "Pass arguments of any type interchangeably", "B", "Varargs (...) let a method accept a variable number of arguments of the specified type, which Java packages into an array accessible inside the method body."),
    ("Classes and Objects", "What is the effect of declaring a class's constructor as 'private'?", "The class can never be instantiated by any code, including its own static methods", "Instances can only be created from within the class itself, commonly used for patterns like Singleton or static factory methods", "It has no effect; constructors cannot be private", "It automatically makes all fields private too", "B", "A private constructor blocks external code from directly instantiating the class via 'new', while the class's own static methods can still call it internally - a common technique for singletons or controlled object creation."),
    ("Inheritance", "What does it mean that Java's 'Object' class is the implicit superclass of every class that doesn't explicitly extend another class?", "Only specially marked classes inherit from Object", "Every class in Java, if it doesn't explicitly extend something else, automatically inherits methods like toString(), equals(), and hashCode() from Object", "Object is just a naming convention with no real inheritance relationship", "This only applies to classes inside java.lang", "B", "Java automatically makes 'Object' the root of the class hierarchy, so every class gets a default (often overridden) implementation of common methods like toString(), equals(), and hashCode() for free."),
    ("Polymorphism", "Why can an ArrayList<Animal> hold both Dog and Cat objects if both classes extend Animal?", "Java automatically converts Dog and Cat objects into Animal objects, losing their specific type", "Because a Dog or Cat 'is-a' Animal through inheritance, so a reference of the more general Animal type can validly point to any of its subclass instances, and the list only cares about that common declared type", "ArrayList can hold any type regardless of declared generics", "This is only possible if Animal is declared as an interface", "B", "Polymorphic references let a supertype variable (or, here, a generic-typed collection) hold any subtype instance, since every Dog and Cat instance genuinely satisfies being an Animal."),
    ("Abstraction", "What compile error occurs if you try to instantiate an abstract class directly, e.g. 'new Animal()' where Animal is abstract?", "No error occurs; abstract classes can always be instantiated", "The compiler rejects it, since abstract classes are explicitly disallowed from direct instantiation - only their concrete subclasses can be instantiated", "It only fails at runtime, not compile time", "It silently creates a Object instance instead", "B", "Java's compiler statically enforces that 'new' cannot be used directly on a class marked abstract, precisely because such a class may have unimplemented (abstract) behavior."),
    ("Interfaces", "What is the purpose of a 'marker interface' like Serializable, which declares no methods at all?", "It has no real purpose and exists only historically", "It signals to the JVM or frameworks that implementing classes opt into certain special treatment or behavior (like being eligible for serialization), purely through the type itself", "It is required syntax for every Java class", "It forces the class to become abstract", "B", "Marker interfaces carry no method contract; their entire purpose is to tag a class with a type that other code (like the serialization mechanism) can check via instanceof to decide how to treat it."),
    ("Exception Handling", "What does wrapping a lower-level exception inside a custom higher-level exception (exception translation) typically accomplish?", "It has no benefit and only adds unnecessary complexity", "It lets calling code depend on a more meaningful, application-specific exception type without needing to know about lower-level implementation details, while often still preserving the original cause via chaining", "It automatically fixes the underlying problem", "It prevents the exception from ever being logged", "B", "Exception translation hides low-level implementation exceptions behind a more meaningful domain-specific exception, improving the API's abstraction, while chaining (via the cause constructor) still preserves the original error for debugging."),
    ("Collections", "What does calling Collections.unmodifiableList(list) return, and what happens if you try to modify the result?", "A fully independent deep copy that can be freely modified without affecting the original", "A read-only view backed by the original list; attempting to add/remove/set elements on it throws an UnsupportedOperationException", "The exact same mutable list object with no behavior change", "It converts the list into an array", "B", "unmodifiableList wraps the original list in a view that blocks structural modification attempts (throwing UnsupportedOperationException), while still reflecting changes made directly to the underlying original list."),
    ("Strings", "What is the output of comparing two Integer objects with '==' when their values are 200, versus when their values are 100, given Java's Integer caching behavior?", "Both comparisons return true", "Integer values from -128 to 127 are cached and reused, so '==' returns true for cached small values like 100, but for 200 (outside the cache range) it typically returns false since separate objects are created", "Both comparisons always return false", "Integer caching applies to all integer values with no range limit", "B", "The JVM caches boxed Integer objects for the range -128 to 127 for performance, so two autoboxed Integers with the same small value can be == equal by coincidence, while values outside that range are typically distinct objects, making == unreliable for comparing Integer values generally (use .equals() instead)."),
    ("Java Basics", "What is the difference between the '==' operator and the '.equals()' method when comparing two custom objects that haven't overridden equals()?", "They always behave identically for any object", "Without an overridden equals(), Object's default equals() implementation just performs reference comparison, so both '==' and '.equals()' check whether they're the exact same object instance", "'.equals()' always compares field values by default for every class", "'==' can only be used with primitive types", "B", "Unless a class overrides equals(), the inherited default from Object simply checks object identity (same as ==), so two logically 'equal' but distinct objects would both be considered unequal by default."),
    ("Data Types", "What is the effect of Java's 'widening' primitive conversion, such as assigning an int value to a long variable?", "It requires an explicit cast and may lose data", "It happens implicitly and safely, since a long can represent every value an int can, with no explicit cast required", "It is not allowed in Java", "It only works for floating-point types", "B", "Widening conversions (like int to long, or float to double) are automatic and safe because the target type can represent the full range of the source type without loss."),
    ("Methods", "What does it mean for a Java method to be 'recursive', and what is a required condition for it to terminate correctly?", "Recursive methods can never terminate", "A recursive method calls itself, and it must have a base case condition that stops further recursive calls, or it will result in a StackOverflowError", "Recursion is only allowed inside static methods", "Recursive methods cannot accept parameters", "B", "A recursive method needs at least one base case that halts further self-calls; without it, each call adds another frame to the call stack until it's exhausted, causing a StackOverflowError."),
    ("Classes and Objects", "What is the purpose of a 'getter' and 'setter' method pair for a private field in Java, as part of encapsulation?", "They serve no real purpose beyond adding extra code", "They provide controlled, indirect access to a private field, letting the class validate or restrict how that field's value is read or changed from outside code", "They make the field directly public", "They are required by the Java compiler for every field", "B", "Encapsulation hides a field's implementation as private and exposes controlled access through public getter/setter methods, letting the class enforce validation rules or change internal representation without breaking external callers."),
    ("Collections", "What does Java's 'Iterator' interface's remove() method allow, that removing directly from a List during a for-each loop does not safely allow?", "There is no difference; both approaches are equally safe", "Iterator.remove() safely removes the current element during iteration by updating the iterator's own internal state, while directly modifying the underlying list during a for-each loop can throw a ConcurrentModificationException", "for-each loops do not support any form of element removal under any circumstances", "Iterator.remove() can only be called before iteration begins", "B", "Because Iterator.remove() is aware of and updates the iterator's own tracking state, it can safely remove the current element mid-iteration, whereas directly calling list.remove() during a for-each loop confuses the iterator's fail-fast modification check and throws ConcurrentModificationException."),
]

JAVA_HARD = [
    ("Java Basics", "What does the JVM's garbage collector fundamentally do, and why doesn't Java generally require manual memory deallocation like C++?", "It has no real function; Java requires manual memory management just like C++", "It automatically identifies and reclaims memory occupied by objects that are no longer reachable from any active reference, removing the need for explicit deallocation like free() or delete", "It only runs once when the program starts", "It deletes all objects at the end of every method call", "B", "The garbage collector periodically traces which objects are still reachable from live references (roots) and reclaims memory for objects that are not, which is why Java code doesn't need explicit free()/delete calls the way C/C++ does."),
    ("Data Types", "What subtlety can cause 'System.out.println(0.1 + 0.2 == 0.3)' to print false in Java?", "Java cannot compare doubles with ==", "Both 0.1 and 0.2 are stored as binary floating-point approximations, and their sum doesn't exactly equal the binary approximation of 0.3, due to how IEEE 754 floating-point represents decimal fractions", "This is a bug specific to certain JVM versions only", "The println method rounds incorrectly", "B", "Like most languages using IEEE 754 floating-point, Java cannot exactly represent many decimal fractions in binary, so arithmetic on doubles can produce tiny rounding discrepancies that make direct == comparisons unreliable."),
    ("Methods", "What compile-time ambiguity can arise from overloading a method with both an 'Object' parameter and an 'int' parameter, when calling it with an Integer object?", "There is never any ambiguity in Java overload resolution", "The compiler must choose the most specific applicable overload based on the argument's compile-time type, and unboxing/widening rules can make the resolution non-obvious, sometimes surprising developers expecting the 'closest' runtime match", "Java always selects the Object overload regardless of argument type", "This situation is a guaranteed compile error", "B", "Overload resolution in Java happens at compile time based on the argument's static type and applies phases (exact match, then widening, then autoboxing, then varargs), which can pick a less 'intuitive' overload than expected if a more specific match exists after considering these phases."),
    ("Inheritance", "Why can calling an overridable instance method from within a superclass's constructor be dangerous?", "Constructors cannot call any methods in Java", "If a subclass overrides that method and the override depends on subclass fields, those fields may not yet be initialized when the superclass constructor runs (since superclass construction happens before subclass field initialization), leading to bugs", "The JVM automatically disables method overriding during construction", "This pattern is forbidden by the compiler and won't build", "B", "Since the superclass constructor runs first (as part of object construction) before the subclass's own field initializers execute, an overridden method invoked from the superclass constructor may operate on not-yet-initialized subclass state."),
    ("Polymorphism", "What is the difference between static binding and dynamic binding in Java, and which one applies to overloaded versus overridden methods respectively?", "Both overloading and overriding always use dynamic binding", "Method overloading is resolved via static binding (at compile time, based on declared types), while method overriding is resolved via dynamic binding (at runtime, based on the object's actual type)", "Overriding uses static binding and overloading uses dynamic binding", "Static and dynamic binding are unrelated to methods entirely", "B", "The compiler picks which overloaded method signature applies based on the compile-time argument types (static binding), while which overridden implementation actually executes is decided at runtime based on the real object type (dynamic binding/dispatch)."),
    ("Interfaces", "What is the functional difference between a 'functional interface' (like Runnable) and a regular interface with multiple abstract methods?", "There is no meaningful difference between them", "A functional interface has exactly one abstract method, which allows it to be implemented concisely using a lambda expression or method reference, unlike interfaces with multiple abstract methods", "Functional interfaces cannot have default methods", "Regular interfaces cannot be implemented by lambda expressions under any circumstances", "B", "Having exactly one abstract method (optionally alongside default/static methods) is what qualifies an interface as 'functional', enabling Java's lambda syntax to provide a concise implementation for that single method."),
    ("Exception Handling", "What subtle issue can occur with a try-with-resources statement if the resource's close() method itself throws an exception, while the try block also threw an exception?", "Only the close() exception is ever reported, and the original exception is silently lost", "The original exception from the try block is the one propagated to the caller, while the close() exception is attached to it as a 'suppressed' exception, both remaining accessible", "Both exceptions cause an immediate JVM crash with no propagation", "try-with-resources ignores exceptions from close() entirely", "B", "Java's try-with-resources mechanism prioritizes the original exception as the primary one thrown, but preserves any exception(s) from closing resources as 'suppressed exceptions' accessible via getSuppressed(), so neither is silently discarded."),
    ("Collections", "Why must a class override both equals() and hashCode() together (not just one) to behave correctly as a key in a HashMap?", "Only equals() needs to be overridden; hashCode() is irrelevant to HashMap", "HashMap uses hashCode() to locate the correct bucket and equals() to confirm an exact match within that bucket - overriding only one can violate their required contract and cause lookups to silently fail even for logically equal keys", "hashCode() is deprecated and no longer used by modern collections", "Overriding either method automatically overrides the other", "B", "The contract requires that equal objects (per equals()) must produce the same hashCode(); breaking this by overriding just one method can mean a key that HashMap places in one bucket (via hashCode) never matches an 'equal' key looked up later, since the bucket search relies on both being consistent."),
    ("Collections", "What does ConcurrentModificationException typically indicate, and why does it occur when using a standard for-each loop with an ArrayList?", "It indicates a corrupted JVM installation", "It's thrown when a collection is structurally modified (e.g. via remove()) while being iterated with an iterator that wasn't the one performing the modification, since Java's fail-fast iterators detect this change via an internal modification counter", "It only occurs when using multiple threads, never in single-threaded code", "It means the collection has exceeded its maximum size", "B", "Fail-fast iterators track a modCount and check it on each next() call; directly modifying the underlying collection's structure outside the iterator's own remove() method changes that counter, causing the iterator to detect the mismatch and throw, even in single-threaded code."),
    ("Strings", "Why does 'new String(\"abc\") == \"abc\"' typically evaluate to false, while '\"abc\" == \"abc\"' typically evaluates to true in Java?", "Both expressions always evaluate to true", "String literals are interned and reused from a shared string pool, so identical literals reference the same object, but 'new String(...)' explicitly forces creation of a distinct object outside that pool", "== always compares String content in Java, regardless of how the object was created", "This behavior is JVM-implementation-specific and undefined by the language", "B", "The JVM maintains a string pool where identical literal values are reused as the same object reference, making == true for two identical literals; explicitly using 'new String(...)' bypasses the pool and creates a separate heap object, so its reference differs even though its content is equal."),
    ("Abstraction", "Why can an abstract class in Java still declare a constructor, even though it can never be instantiated directly?", "This is not actually allowed; abstract classes cannot have constructors", "The constructor exists to be called via 'super()' from concrete subclasses, allowing the abstract class to still initialize its own fields as part of any subclass's object construction", "The constructor is only used for documentation purposes and has no runtime effect", "Abstract class constructors are automatically deleted at compile time", "B", "Even though 'new AbstractClass()' is disallowed, subclass constructors implicitly or explicitly call the abstract superclass's constructor via super(), so it still plays a real role in initializing the shared parts of the object's state."),
    ("Java Basics", "What is the significance of the JVM's 'stack' versus 'heap' memory areas for a typical method call involving local primitive variables and an object argument?", "Both areas store identical data with no distinction", "Local primitive variables and method-call frames live on the stack (fast, per-thread, automatically reclaimed on method return), while objects themselves are allocated on the shared heap (reclaimed later by the garbage collector)", "The heap is only used for static variables", "The stack has no size limit, unlike the heap", "B", "Each thread has its own call stack holding local variables and frame data that's cleaned up automatically as methods return, while actual object instances live on the heap, shared across threads, and are reclaimed only once no references remain (via garbage collection)."),
    ("Inheritance", "What does 'covariant return type' mean when overriding a method in Java, and why is it allowed?", "It means the overriding method must return exactly the same type as the parent method, with no exceptions", "It means an overriding method may return a subtype of the return type declared in the overridden superclass method, since any caller expecting the superclass's return type can still safely accept the more specific subtype", "It is not supported by Java at all", "It only applies to primitive return types", "B", "Since a subtype 'is-a' supertype, returning a more specific type from the override doesn't violate the contract callers rely on (they still get something assignable to the original declared return type), so Java permits narrowing the return type in an override."),
    ("Polymorphism", "What subtle compile-time versus runtime distinction explains why 'Animal a = new Dog(); a.someStaticMethod();' does NOT exhibit polymorphic dispatch the way an instance method call would?", "Static methods are always resolved polymorphically, identically to instance methods", "Static methods are resolved based on the reference variable's declared (compile-time) type, not the object's actual runtime type, since static methods belong to the class itself rather than being dispatched per-instance", "This code fails to compile under any circumstances", "Static methods cannot be called on object references at all", "B", "Because static methods aren't part of an object's dynamically dispatched vtable, calling one through a reference uses that reference's declared type at compile time - if Dog overrides (technically 'hides') that static method, a.someStaticMethod() still resolves to Animal's version."),
    ("Exception Handling", "Why is catching 'Throwable' generally discouraged compared to catching 'Exception'?", "Throwable does not exist as a class in Java", "Throwable is the superclass of both Exception and Error, and Error subclasses (like OutOfMemoryError or StackOverflowError) typically represent serious JVM-level problems that application code usually shouldn't attempt to handle or recover from", "Catching Throwable is functionally identical to catching Exception in every way", "Error subclasses can never actually be thrown by the JVM", "B", "Error represents severe, often unrecoverable conditions the JVM itself signals (like running out of memory), and swallowing these by catching Throwable broadly can mask serious problems or attempt inappropriate recovery from a state the application cannot safely continue in."),
    ("Java Basics", "Why does modifying a 'final' object reference's fields not violate the 'final' guarantee, e.g. 'final List<String> list = new ArrayList<>(); list.add(\"x\");'?", "This code is actually invalid and won't compile", "'final' on a reference variable only prevents reassigning the variable to point to a different object; it says nothing about the mutability of the object it already points to", "final automatically makes every object it references immutable", "list.add() secretly creates a new list and reassigns it", "B", "final locks the reference itself (you can't do list = new ArrayList<>() again), but the object being referenced can still be freely mutated through its own methods, since immutability of a reference and immutability of an object are separate concerns."),
    ("Data Types", "What subtle bug can occur from comparing two double values for exact equality after arithmetic operations, and what is the conventional fix?", "Doubles can always be compared safely with == after any operation", "Floating-point arithmetic can introduce tiny rounding errors, so exact == comparison may unexpectedly fail; the conventional fix is comparing whether the absolute difference is within a small acceptable epsilon", "The fix is to always convert doubles to int before comparing", "This only affects float, never double", "B", "Because IEEE 754 double arithmetic can't represent every decimal value exactly, two computations that are mathematically equal can differ by a tiny margin - so comparing with 'Math.abs(a - b) < epsilon' is the standard workaround instead of ==."),
    ("Loops", "What subtle performance issue can arise from concatenating strings with '+' inside a loop that runs many iterations, at the bytecode level?", "String concatenation with + is always optimized identically to StringBuilder in every context", "In a loop, the compiler typically cannot always safely collapse repeated '+' concatenations into a single StringBuilder the way it could within a single non-looping expression, potentially creating a new StringBuilder (and String) on every iteration", "This has no performance impact since Strings are stored on the stack", "The JVM automatically parallelizes loop-based string concatenation", "B", "While the compiler can optimize a single concatenation expression into one StringBuilder chain, doing this inside a loop across iterations often still results in a fresh StringBuilder (and resulting String) being created and discarded each pass, which is why an explicitly reused StringBuilder outside the loop is recommended for performance-sensitive code."),
    ("Methods", "What does 'method hiding' mean for static methods in Java, and how does it differ from overriding for instance methods?", "Static methods can be overridden exactly like instance methods, with identical dynamic dispatch behavior", "When a subclass declares a static method with the same signature as a superclass's static method, it 'hides' rather than overrides it - which version runs is determined by the reference's compile-time type, not the object's runtime type", "Static methods cannot share a name with a superclass's static method at all", "Method hiding only applies to private methods", "B", "Because static methods belong to the class rather than being part of an object's dynamically dispatched behavior, a same-signature static method in a subclass merely hides the superclass version at the class level; resolution uses the compile-time reference type rather than the runtime object type, unlike true overriding."),
    ("Classes and Objects", "Why can accessing an uninitialized instance field from within a constructor sometimes return an unexpected default value instead of a value you expect a field initializer to have set?", "Field initializers always run before the constructor body starts, so this cannot happen", "The order of initialization matters: if the constructor code accessing the field runs (e.g. via a call chain) before that specific field's initializer executes in source order, it will still hold its default value at that point", "Uninitialized fields cause a compile-time error, not a runtime default", "This behavior is randomized by the JVM", "B", "Java initializes instance fields and instance initializer blocks in the textual order they appear, interleaved with the constructor body; if logic (directly or via a called method) reads a field before its own initializer runs in that sequence, it observes the type's default value rather than the intended initial value."),
    ("Inheritance", "What issue can arise if a subclass overrides equals() but not hashCode(), when instances are later stored in a HashSet?", "There is no issue; HashSet only relies on equals()", "The set may end up storing what look like 'duplicate' logical entries, or fail to find an entry that should logically be 'equal', because HashSet uses hashCode() to locate the bucket before ever calling equals() to confirm a match", "hashCode() is automatically regenerated to match whenever equals() is overridden", "This only matters for TreeSet, not HashSet", "B", "Since HashSet first computes hashCode() to determine which bucket to search, and only then uses equals() within that bucket, two objects that your overridden equals() considers equal but that still produce different inherited hashCode() values can end up in different buckets, breaking expected set behavior."),
    ("Polymorphism", "Why is it generally considered better practice to declare a variable's type as an interface (e.g. List<String> list = new ArrayList<>();) rather than the concrete implementation type?", "There is no practical benefit; it is purely stylistic with zero effect on code", "Coding to the interface type decouples the rest of the code from the specific implementation, making it easier to swap implementations later (e.g. switching to LinkedList) without changing code that only relies on the List contract", "ArrayList cannot be assigned to a List-typed variable at all", "Interface-typed variables execute faster at runtime than concrete-typed variables", "B", "By depending only on the interface's contract rather than implementation-specific details, code becomes more flexible and easier to refactor, since swapping the concrete implementation later requires changing only the object creation line, not every place the variable is used."),
    ("Abstraction", "In a class hierarchy using the Template Method pattern, where an abstract class defines a concrete method that calls several abstract methods internally, what is the purpose of this design?", "It is purely for organizing code alphabetically, with no behavioral purpose", "It lets the abstract superclass define a fixed overall algorithm/sequence of steps, while deferring the specific implementation details of certain steps to subclasses, ensuring the overall structure stays consistent", "It prevents subclasses from ever overriding any method", "It removes the need for subclasses to implement any methods at all", "B", "The Template Method pattern centralizes the invariant parts of an algorithm's structure in the abstract class's concrete method, while the variable, subclass-specific parts are delegated to abstract methods that each subclass fills in - keeping the overall flow consistent across all subclasses."),
    ("Interfaces", "What compilation issue can arise from a diamond-shaped interface hierarchy where interface D extends both B and C, and B and C both extend a common interface A with a default method, if B and C each override that default method differently?", "Java always resolves this automatically with no developer intervention needed", "D inherits two conflicting default method implementations, and the compiler requires D (or its implementing class) to explicitly override that method to resolve the ambiguity", "This scenario is impossible since interfaces cannot extend multiple interfaces", "The most recently compiled interface's version is silently chosen", "B", "Java doesn't silently pick a 'winner' between two different inherited default implementations reaching the same interface via different paths - it forces an explicit override to resolve the ambiguity, avoiding the classic diamond problem's implicit resolution."),
    ("Exception Handling", "What is the risk of using exceptions purely for ordinary, expected control flow (e.g. using an exception to signal 'end of valid input' in a tight loop) rather than for truly exceptional conditions?", "There is no downside; exceptions are always the most efficient control-flow mechanism", "Constructing an exception (particularly filling in its stack trace) has real performance overhead compared to normal conditional checks, and using exceptions for routine, expected outcomes obscures the code's intent and can hurt performance in hot code paths", "Exceptions cannot be used inside loops at all", "This pattern is required by the Java Language Specification for input validation", "B", "Creating an exception object typically captures a full stack trace, which is comparatively expensive; relying on exceptions for routine, frequently expected outcomes both adds unnecessary overhead in performance-sensitive code and makes the code's normal-case logic harder to follow compared to simple conditional checks."),
    ("Collections", "What subtle difference exists between Vector/Hashtable and ArrayList/HashMap regarding thread safety, and why might that make ArrayList/HashMap 'faster' for single-threaded use?", "There is no difference; all four classes behave identically", "Vector and Hashtable synchronize their methods internally (thread-safe but with locking overhead), while ArrayList and HashMap are unsynchronized, avoiding that locking cost when thread safety isn't needed", "ArrayList and HashMap are actually the thread-safe versions, and Vector/Hashtable are not", "Synchronization only affects read operations, never writes", "B", "The older Vector/Hashtable classes synchronize nearly every method for safe concurrent access, which adds locking overhead even in single-threaded code; the newer ArrayList/HashMap intentionally skip that synchronization for better raw performance, leaving thread safety to be handled explicitly (e.g. via Collections.synchronizedList or concurrent collections) only when actually needed."),
    ("Strings", "Why can String.intern() be relevant when working with a large number of duplicate string values built at runtime (not as literals)?", "intern() has no practical effect on memory or object identity", "intern() returns a canonical representative from the JVM's string pool for a given string's content, letting logically identical runtime-built strings share a single pooled object instead of each being a separate heap object, which can reduce memory usage", "intern() converts a String into a StringBuilder for further mutation", "intern() is only usable on String literals, never on runtime-built strings", "B", "Strings built at runtime (e.g. via concatenation or StringBuilder) don't automatically go into the shared string pool the way literals do; calling intern() explicitly adds (or retrieves) a pooled equivalent, which can meaningfully reduce memory footprint when many duplicate runtime strings exist, at the cost of pool lookup overhead."),
    ("Java Basics", "What does the JVM's 'method area' (part of what's sometimes loosely called 'permgen' or 'metaspace' depending on JVM version) primarily store?", "Only local variables for currently executing methods", "Per-class structural data such as the runtime constant pool, field and method metadata, and the code for methods themselves, shared across all instances of that class", "Only garbage-collected object instances", "Only primitive values used inside loops", "B", "Unlike the per-thread stack (locals) or the heap (object instances), the method area holds class-level structural metadata - things like method bytecode and the constant pool - loaded once per class and shared by everything using that class."),
    ("Data Types", "What does Java's 'implicit narrowing' restriction mean for something like 'byte b = 200;' failing to compile, versus 'byte b = 100;' compiling successfully?", "Both statements always fail to compile, since assigning any literal to byte is disallowed", "For constant expressions assigned directly to a narrower type, the compiler checks whether the constant actually fits within that type's range (byte: -128 to 127); 200 doesn't fit and requires an explicit cast, while 100 does fit and compiles without one", "byte can never store a literal value under any circumstances", "This behavior only applies to variables declared as final", "B", "Java makes a special allowance for assigning int-typed constant expressions directly to a narrower type like byte or short, but only when the compiler can verify the literal value actually fits in that narrower range at compile time - 200 exceeds byte's max of 127, so it requires an explicit cast, unlike 100."),
    ("Loops", "Why might an infinite loop 'for(;;)' combined with an internal break condition sometimes be intentionally preferred over an equivalent while(condition) loop?", "for(;;) is always faster at the bytecode level than while(true)", "Sometimes the natural exit condition doesn't cleanly fit at the top of the loop (e.g. it depends on work done partway through each iteration), so an internal break after that point can express the intended control flow more clearly than restructuring the condition to fit a while-loop's upfront check", "while(condition) loops cannot contain break statements", "This pattern is deprecated and no longer compiles in modern Java", "B", "When the natural place to check 'should we stop' logically falls in the middle of the loop body's work (not cleanly at the very start), an infinite loop with an internal break can express that intent more directly than contorting the loop's front condition to account for state that isn't available yet at that point."),
    ("Methods", "What does it mean that Java arguments are 'evaluated left to right', and why does this matter for an expression like 'compute(sideEffectA(), sideEffectB())'?", "Argument evaluation order in Java is unspecified and can vary between JVM implementations", "Java guarantees sideEffectA() fully completes (including any side effects) before sideEffectB() begins evaluating, giving deterministic, predictable ordering for code relying on the sequence of side effects", "Only the last argument in a method call is actually evaluated", "Arguments are evaluated in a different, reversed order for varargs methods", "B", "Unlike some languages that leave argument evaluation order unspecified, the Java Language Specification guarantees strict left-to-right evaluation of a method call's arguments, so any side effects in sideEffectA() are guaranteed to occur before sideEffectB() begins."),
    ("Classes and Objects", "What is the risk of overriding equals() based only on some, but not all, of an object's meaningful fields?", "There is no risk since equals() only needs to check a single arbitrary field", "Two objects that differ in a field not included in the comparison would be considered 'equal', which can silently violate the intuitive expectation that equals() reflects full logical equivalence, causing subtle bugs in collections or comparisons relying on it", "Java automatically includes every field in equals() regardless of what you write", "This only matters for classes with more than 10 fields", "B", "Since equals() defines what 'the same' means for your objects, omitting fields that matter to your domain's notion of equality can cause objects that a user would consider meaningfully different to be treated as identical by anything relying on equals() (like removing 'duplicates' from a collection), leading to subtle correctness bugs."),
    ("Inheritance", "Why does Java disallow overriding a method to throw a broader checked exception than the one declared in the superclass's version?", "Java has no such restriction; any exception can be thrown from an override", "This would break the Liskov substitution principle for callers relying on the superclass's method signature - code catching only the superclass's declared checked exceptions could be caught off guard by an unanticipated broader checked exception from the override", "Broader checked exceptions are only disallowed for interface methods, not superclass methods", "This restriction only applies to unchecked exceptions", "B", "Because callers of the superclass type only prepare (via try/catch or throws) for the checked exceptions declared on that method, allowing an override to declare a broader checked exception would silently break that contract for any code polymorphically calling the method through the superclass reference."),
    ("Polymorphism", "What does it mean for polymorphic dispatch to have a small but measurable runtime cost compared to a direct (non-virtual) method call, and why doesn't this usually matter in practice?", "Polymorphic calls are always exactly as fast as static calls with zero overhead in every JVM", "Resolving which overridden method to call typically involves a virtual method table lookup at runtime, which is marginally slower than a direct call; however modern JIT compilers can often optimize this away (e.g. via inlining) when the actual type is predictable, making the overhead negligible for most code", "Polymorphic method calls cannot be executed by the JVM at all without an interpreter", "This overhead only exists for static methods, not instance methods", "B", "Dynamic dispatch does carry an inherent extra indirection compared to a statically resolved call, but modern JIT compilers use techniques like inline caching and speculative devirtualization to largely eliminate this cost in hot code paths where the actual runtime type is stable or predictable, which is why the theoretical overhead rarely matters for typical application code."),
    ("Abstraction", "What subtle backward-compatibility problem did Java 8's introduction of default methods in interfaces specifically solve for existing, widely-used interfaces like Collection?", "Default methods have nothing to do with backward compatibility", "Before default methods, adding any new abstract method to a widely-implemented interface (like Collection) would break every existing implementing class that didn't define it; default methods let new methods (like stream()) be added with a built-in fallback implementation, avoiding that breakage", "Default methods removed the need for interfaces to declare any methods at all", "This feature only affects classes, not interfaces, and has no relation to interfaces like Collection", "B", "Prior to Java 8, extending a public interface with a new method would force every single implementing class across the ecosystem to add an implementation or fail to compile; default methods let the JDK evolve core interfaces like Collection (adding things like stream()) while existing implementations kept compiling, inheriting the interface's default behavior automatically."),
    ("Exception Handling", "What does it mean that unchecked exceptions (RuntimeException and its subclasses) 'bypass' the compiler's checked-exception enforcement, and what design trade-off does this represent?", "Unchecked exceptions can never actually be thrown at runtime", "The compiler doesn't require callers to catch or declare unchecked exceptions, trading compile-time enforced handling for flexibility - appropriate for programming errors (like passing an invalid argument) that shouldn't force every caller up the chain to explicitly handle them", "Unchecked exceptions are resolved entirely at compile time with no runtime cost", "This distinction was removed in modern versions of Java", "B", "The checked/unchecked distinction is a deliberate design trade-off: checked exceptions force deliberate handling for conditions callers can reasonably be expected to recover from (like a missing file), while unchecked exceptions represent programming errors or truly exceptional conditions where forcing every intermediate caller to declare/catch them would add clutter without meaningful benefit."),
    ("Collections", "What is the practical implication of choosing TreeMap over HashMap when you need both fast average-case lookups and a sorted iteration order?", "There is no trade-off; TreeMap and HashMap perform identically in every scenario", "TreeMap maintains keys in sorted order (via a red-black tree) with O(log n) operations, while HashMap offers faster average O(1) operations but no ordering guarantee - the choice depends on whether sorted iteration is worth the additional logarithmic overhead", "HashMap always maintains sorted order automatically", "TreeMap cannot be iterated over at all", "B", "TreeMap's red-black tree backing gives it guaranteed sorted-key iteration at the cost of O(log n) typical operation time, while HashMap's hash-table backing gives faster average O(1) operations but sacrifices any predictable ordering - picking between them depends on whether the application genuinely needs sorted traversal."),
    ("Strings", "Why can code that repeatedly calls String.split() with a complex regular expression inside a performance-critical loop become a bottleneck, and what is a common mitigation?", "split() has no performance cost regardless of how it's used", "Each call to split() with a regex pattern string typically compiles that pattern internally on every invocation; pre-compiling the pattern once with Pattern.compile() and reusing it avoids repeated compilation overhead", "split() can only be called once per String object ever", "The mitigation is to avoid using regular expressions in Java entirely", "B", "Passing a regex string directly to split() causes the underlying Pattern to be compiled fresh on every call; in a hot loop, pre-compiling the Pattern once outside the loop and reusing its matcher/split logic avoids that redundant compilation cost on each iteration."),
    ("Java Basics", "Why does modifying the value of a boxed Integer object used as a HashMap key (if it were somehow made mutable) illustrate a broader principle about why mutable objects generally make poor hash keys?", "Integer is mutable in Java, so this scenario applies directly", "If a key object's hashCode()-relevant state changes after insertion, the key may become unreachable in its original bucket location, since the map cannot know to relocate the entry - which is why keys are conventionally expected to be effectively immutable", "HashMap automatically re-buckets every entry whenever any object anywhere is mutated", "This principle only applies to TreeMap, not HashMap", "B", "A hash-based structure like HashMap places entries into buckets based on the key's hashCode() at insertion time; if that value could change afterward (mutable state affecting hashCode), the entry would remain in its original, now 'wrong' bucket, making it unfindable via a fresh lookup - hence the strong convention that hash keys should be effectively immutable, and why Integer specifically is immutable in Java."),
    ("Loops", "What is the conceptual difference between a loop invariant and a loop's termination condition, and why do experienced developers often reason about both explicitly when writing complex loops?", "They are the same concept with different names", "A loop invariant is a condition that remains true before and after every iteration (helping reason about correctness throughout execution), while the termination condition determines when the loop stops; explicitly reasoning about both helps catch off-by-one errors and ensure the loop both behaves correctly and actually halts", "Only the termination condition matters; invariants are a purely academic concept with no practical use", "Loop invariants only apply to recursive functions, not iterative loops", "B", "A loop invariant captures what should always hold true throughout the loop's execution (useful for proving correctness), while the termination condition governs when iteration stops - reasoning about both separately helps developers avoid subtle bugs like off-by-one errors or loops that never terminate despite seemingly correct logic."),
]
